### Set Up

To start the mongo server run:
```sh
$ cd backend/
$ mongod
```
To start the backend run:
```sh
$ cd backend/
$ nodemon app
```

To start the frontend run:
```sh
$ cd frontend/
$ npm start
```
